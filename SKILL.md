---
name: code-quality-check
description: 代码质量检测（多语言通用）。运行标准 linter/静态分析工具并结合 AI 深度审查，输出带 0-100 评分的 Markdown 质量报告。支持 Python、JavaScript/TypeScript、Java、Go、Rust、C/C++、Shell 等语言，按文件类型自动识别。触发词："代码质量检测"、"代码检查"、"代码审查"、"code review"、"代码质量怎么样"、"帮我看看这段代码有没有问题"、"lint 一下这个项目"、"代码评分"、"安全扫描代码"、"上线前检查一下代码"。
---

# 代码质量检测

对目录或代码片段执行"工具检测 + AI 审查"两级分析，产出评分与分级问题清单。

## 工作流

### 第 1 步：扫描项目

运行扫描脚本（自动识别语言、统计文件/行数、检测可用的 lint 工具，`--run` 会执行已安装的工具并收集原始告警）：

```bash
python3 scripts/scan.py <目标目录> --run
```

- 脚本输出 JSON：`languages`、`large_files_over_500_lines`、`tools_available`、`tools_missing`、`tool_findings`。
- 大项目（>2000 文件）先用 `--max-files 2000` 采样，或让用户指定子目录。
- 用户只给了一个代码文件/片段时跳过本步，直接进入第 3 步 AI 审查。

### 第 2 步：补齐关键工具（可选）

若 `tools_missing` 中包含该语言的主流工具，且安装成本低（pip/npm 单包），可快速安装后重跑：

| 语言 | 首选工具 | 安装 |
|------|---------|------|
| Python | ruff, bandit | `pip install ruff bandit` |
| JS/TS | eslint | 项目内 `npx eslint`（需已有配置）|
| Shell | shellcheck | 系统包管理器 |
| C/C++ | cppcheck | 系统包管理器 |
| 通用（安全） | semgrep | `pip install semgrep` |

安装失败或耗时过长就跳过，在报告中注明"未检测项"。不要为了检测修改项目的依赖配置。

### 第 3 步：AI 深度审查

工具覆盖不到的问题由 AI 阅读代码补充。按代码量选择策略：

- **≤30 个文件**：通读全部代码文件。
- **>30 个文件**：优先阅读入口文件、核心业务模块、scan.py 报告的大文件（>500 行）以及工具告警集中的文件；其余抽查。

审查清单（按优先级）：

1. **安全**：注入（SQL/命令/XSS）、硬编码密钥/token/密码、不安全反序列化、路径穿越、越权访问、危险配置（如 `debug=True`、CORS 全开）。
2. **正确性**：未处理的异常与错误返回、空值解引用、资源未关闭（文件/连接/锁）、并发竞争、边界条件、逻辑死代码。
3. **可维护性**：超长函数（>100 行）、深层嵌套（>4 层）、复制粘贴重复块、循环依赖、魔法数字、命名与实现不符、注释过期。
4. **风格一致性**：工具告警汇总（去重、剔除误报后），import 混乱、命名风格混杂。
5. **工程化**：有无测试及测试质量、构建/依赖配置问题（版本钉死缺失）、CI 缺失。

对工具告警必须复核：确认是真问题才计入报告，误报（如生成代码、已有防护的告警）剔除并可注明。

### 第 4 步：评分与报告

1. 读取 [references/rubric.md](references/rubric.md)，按五维度加权计算 0-100 总分并给出 A-F 评级。评分必须与问题清单一致、可追溯（每个维度列出扣分明细）。
2. 按 [references/report-template.md](references/report-template.md) 的模板生成报告，保存到 `output/<项目名>_代码质量报告.md`。
3. 向用户展示报告：先给总分和一句话结论，再列严重/重要问题（最多各 5 条），完整内容指向报告文件。

## 注意事项

- 只做检测与报告，**不主动修改被测代码**；用户明确要求修复时才改，且逐项确认。
- 报告文件、评分表、问题定级必须使用中文（代码标识符除外）。
- 每条问题必须带 `文件:行号`；无法定位的行级问题时给出函数名。
- 检测发现硬编码密钥时，报告中只写位置和类型，**不要复述密钥内容**。
- 项目里有 `.eslintrc`、`pyproject.toml [tool.ruff]`、`.clang-format` 等既有配置时，尊重项目配置运行工具，不要另立规则强加于人。

## 资源

- `scripts/scan.py` — 语言识别、行数统计、可用工具探测与批量执行（详见脚本 docstring）。
- `references/rubric.md` — 五维度评分标准、扣分细则、A-F 评级映射。
- `references/report-template.md` — 报告结构与输出要求。
